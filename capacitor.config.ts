import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.list_app',
  appName: 'list_app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
